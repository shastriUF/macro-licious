NODE_ENV=development
PORT=4000
APP_NAME=MacroLicious API
MAGIC_LINK_TTL_MINUTES=15

# Auth provider mode: dev | supabase
AUTH_PROVIDER=dev

# Supabase config (required when AUTH_PROVIDER=supabase)
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
# Keep secret out of .env; export it in shell instead:
# export SUPABASE_SECRET_KEY=
SUPABASE_EMAIL_REDIRECT_URL=
